In the folder containing your Spring Boot project, run the following command in the terminal:
mvnd spring-boot:run
Once the application starts, open your browser and go to:
http://localhost:8080/quiz.html
